# ===========================================
# NERRF Stack - Environment Variables
# ===========================================
# Copy this file to create your .env for local development
# For Dokploy: Set these in the Dashboard instead
# ===========================================

# ===== FRONTEND (Build Arguments) =====
# Set this in Dokploy Build Arguments for frontend service
REACT_APP_API_URL=https://api.yourdomain.com

# ===== DATABASE =====
POSTGRES_USER=imageuser
POSTGRES_PASSWORD=change_this_to_secure_password_in_production
POSTGRES_DB=imagedb

# ===== BACKEND API =====
# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===== NOT NEEDED IN .ENV (Auto-configured internally) =====
# These are set automatically in docker-compose.prod.yml:
# - REDIS_URL=redis://redis:6379/0
# - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
# - ELASTICSEARCH_URL=http://elasticsearch:9200
# - MAX_UPLOADS_PER_IP=25
